package Scanner;

//Scanner er en indbygget klasse i Java, man bruger til bruger inputs
//Scanner klassen kan importeres ved at skrive: import java.util.Scanner;
//Husk altid at bruge try - catch ved fillæsning
public class Scanner {
/*
    Scanner scan = new Scanner(System.in); // Scanner objekt

Eksempel på en Scanner med almindelig bruger inputs

import java.util.Scanner;  // Import the Scanner class

    Scanner myObj = new Scanner(System.in);  // Create a Scanner object
    System.out.println("Enter username");

    String userName = myObj.nextLine();  // Read user input
    System.out.println("Username is: " + userName);  // Output user input


Eksempel på en Scanner med fil læsning

    try {
      File myObj = new File("filename.txt");
      Scanner myReader = new Scanner(myObj);
      while (myReader.hasNextLine()) {
        String data = myReader.nextLine();
        System.out.println(data);
      }
      myReader.close();
    } catch (FileNotFoundException e) {
      System.out.println("An error occurred.");
      e.printStackTrace();
    }

 */
}